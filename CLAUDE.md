# Is My Train Screwed? — Project Context

Full spec: `is-my-train-screwed-spec.md` (read it for anything not covered below — this file is orientation, not a substitute).

## What this is

Conversational NYC transit trip advisor (PWA). Differentiates on judgment, not routing: empirical (never LLM-guessed) transfer-miss probabilities, proactive monitored-trip re-planning, accessibility/luggage-aware routing, and a data-gated airport mode (LGA first). Beachhead users: Cornell Tech / Roosevelt Island subway commuters.

## Hard rules (violating these is a spec bug, not a style nit)

- **LLM agents never compute numbers, routes, or probabilities.** They call tools and narrate tool output only. If you catch yourself writing a prompt that asks a model to produce a number, stop — the fix is always better data or an honest "insufficient data (n<200)" response.
- **Model choice is a config/env parameter per agent, never hardcoded.** Default Haiku 4.5 everywhere; never Opus/flagship tier for anything in this product.
- Bus corridor probabilities/airport mode only surface once that corridor's data crosses `n≥200` observations — data-gated, not date-gated.
- The only autonomous write path any agent has is appending a validated entry to bus-corridor collector config (Coverage-Expansion Agent). Everything else escalates to Frank.
- Never mock/stub/hardcode fake credentials "to keep moving." If something's missing, build what doesn't depend on it and flag what's blocked — don't fake it.

## Reference documents (read these before re-deriving their contents)

- `REUSE.md` — mini-nyc-3d (`F:\Cornell Tech\mini-nyc-3d`) reuse audit. Confirmed-live subway GTFS-RT endpoint + feed paths, the `N`/`S` stop-suffix quirk, and the finding that mini-nyc-3d has **no bus real-time integration** to port (a GTFS-RT decode pattern exists and is reusable; SIRI has nothing to port).
- `docs/superpowers/plans/2026-08-14-is-my-train-screwed-roadmap.md` — Phase 1 bite-sized implementation plan (file-level, TDD, complete code) plus Phase 2-5 roadmap scope. Written via the superpowers:writing-plans skill; execute via subagent-driven-development or executing-plans.

## Credentials status (as of 2026-08-14)

All secrets live in `backend/.env` (gitignored, never committed — verified `git status` excludes it before every commit).

| Credential | Status | Env var |
|---|---|---|
| MTA BusTime API key | ✅ have it, live-tested (HTTP 200 on all 3 GTFS-RT bus endpoints: tripUpdates, vehiclePositions, alerts) | `MTA_BUSTIME_API_KEY` |
| Anthropic API key | ✅ have it, live-tested against `claude-haiku-4-5-20251001` (cheapest model — use this everywhere per user instruction, matches spec §9.1 default anyway) | `ANTHROPIC_API_KEY` |
| GitHub token | ✅ rotated 2026-08-14 to a fine-grained PAT scoped to just `is-my-train-screwed`: Metadata (read, mandatory minimum) + Issues (read/write), 90-day expiry — this is what Phase 4's Feedback Triage Agent will use. Verified via API: reads issues (200), cannot do anything Contents-related (the token grants nothing there — the earlier "contents read worked" result was the repo being *public*, readable with zero auth, not a token-scope leak). `git push`/`pull` are unaffected by this token entirely — they authenticate via a separate Windows Credential Manager entry (`git config credential.helper` = `manager`), never via `.env`. | `GITHUB_TOKEN` |
| Email API key (Resend/Postmark) | ❌ not yet provided — user will supply after hosting is live. Blocks Phase 4 only | `RESEND_API_KEY` or `POSTMARK_API_KEY` |
| Hosting account (Railway free tier) | ❌ not yet provided — user will supply after hosting is live. Blocks public deploy only, not local dev/collection | n/a |
| MTA subway GTFS-RT | ✅ confirmed live, no key required: `https://api-endpoint.mta.info/Dataservice/mtagtfsfeeds/{feed}` | n/a |
| MTA bus GTFS-RT | ✅ confirmed live 2026-08-14: `https://gtfsrt.prod.obanyc.com/{tripUpdates,vehiclePositions,alerts}?key=...`. Real route_id values confirmed: `Q70+`, `M60+` (the `+` = SBS branding). **Chosen over SIRI StopMonitoring** — see `REUSE.md` §5 | n/a |
| MTA subway/bus static GTFS zip URL | ✅ confirmed live 2026-08-24 (Phase 1 Task 2): subway `web.mta.info/developers/data/nyct/subway/google_transit.zip`; bus is **`web.mta.info/developers/data/busco/google_transit.zip`** — NOT `nyct/bus` (404s), agency segment is `busco`. Both confirmed by real download + content inspection (subway: real A/C/E/B/D routes under `MTA NYCT`; bus: real Queens routes under `MTABC`, all 9 standard GTFS files, correct sizes). HTTP-level + content confirmation only, not cross-referenced against MTA's documented developer portal listing. | n/a |
| subwaydata.nyc backfill | ✅ confirmed live, data back to 2021-04-01, daily ~7am | n/a |
| NYC OSM street extract (for OTP's street layer — needed for `plan_route` to snap arbitrary lat/lon to the graph at all, not related to any MTA credential) | ✅ downloaded 2026-08-25 from BBBike's pre-clipped NYC extract: `download.bbbike.org/osm/bbbike/NewYork/NewYork.osm.pbf` (~150MB, all 5 boroughs). Lives at `backend/data/otp/NewYork.osm.pbf`, gitignored like the GTFS zips — re-download from that URL if `backend/data/` is ever wiped. Not from Geofabrik (whose smallest relevant extract is the whole NY state, ~10x larger, unnecessary for this scope). | n/a |
| MTA subway GTFS-RT trip-matching in OTP | ✅ **fixed 2026-08-25 (Task 11)** — corrects a wrong conclusion an earlier session reached (below). Root cause was never "architecturally unfixable": it's a simple, mechanical trip_id format mismatch. MTA's real-time trip_id (e.g. `052250_6..N03R`) is the static trip_id (e.g. `BSP26GEN-A087-Weekday-00_052250_6..N03R`) with everything up to and including the first `_` stripped off. Verified live: 90% of real RT trip_ids exact-suffix-match a real static trip_id. The fix is `backend/app/realtime_proxy.py`, a small FastAPI proxy that sits between OTP and the raw MTA feed, resolves each trip_update's short RT trip_id to its matching long static trip_id (using calendar-based disambiguation for suffixes shared by multiple static trips, never guessing), and re-serves the rewritten protobuf; OTP's 8 subway updaters now point at the proxy (`http://host.docker.internal:8090/rt/{feed}`) instead of the raw MTA URLs. Confirmed live: the proxy correctly rewrites ~89-90% of trip_ids (measured both via the standalone `TripIndex` class and via the running proxy's HTTP output). With the proxy in place, OTP's `ResultLogger` success rate jumped from Task 9's flat 0.0% on all 8 feeds to a real, stable (checked across 2 consecutive 30s poll cycles) mixed picture: 6 of 8 feeds matched substantially (~41-88%), 2 feeds stayed at a consistent 0%. **Identified and root-caused same day, post-Task-11**: the 2 stuck feeds are **L and Staten Island Railway (SI)** — confirmed by independently fetching each of the 8 feeds and checking suffix-match rate directly (BDFM alone: 73%, ACE: 86%, G: 89%, JZ: 52%, NQRW: 79%, numbered: 48% — all healthy; L: 0/35, SI: 0/9). Root cause: L and SIR's real-time trip_ids drop the trailing path/shape code the other 6 feeds include (`064100_L..N` vs. the standard `064100_L..N01R`) — plausibly because the L train runs CBTC (a different signaling system from the rest of the subway) and SIR is a separate operating division, both likely generating real-time data through a different backend than the standard subway RT pipeline. This is a real, understood, buildable gap (a second-tier time+route+direction fallback match, same never-guess discipline as the current proxy) — not "inaccessible." **Deliberately left unfixed**: neither L nor SIR touches this project's actual beachhead scope (Roosevelt Island/Cornell Tech is served by F, not L; the LGA corridors are Q70/M60 bus, not subway) — decided with the user 2026-08-25 to document and defer rather than build the fix now. Revisit if L or SIR ever becomes relevant to the product. This also resolves the apparent Roosevelt Island/Lex-63 (F/BDFM) near-miss noted in Task 11's own report — BDFM's real match rate is 73%, so that was one unlucky trip at query time, not a feed-level problem.

End-to-end GraphQL verification (`fromPlace`/`toPlace`, same technique as Task 9/10) confirmed `realTime: true` with genuine non-zero `arrivalDelay` values on A/C/E and numbered-line queries; the Roosevelt Island↔Lex/63 (F train, BDFM feed) pair used throughout this project still returned `realTime: false` in this specific test — an honest partial result, not a 100% win, consistent with the per-feed match rates. Full numbers in `.superpowers/sdd/task-11-report.md`. The earlier "known non-functional, architecturally unfixable, defer to a future `nyct-gtfs` rebuild" conclusion (previously documented here and in `.superpowers/sdd/progress.md`'s Task 9 notes) inferred causation from a `direction_id`-missing correlation without testing whether the trip_ids were actually matchable — they were, once reformatted. That plan (a separate Phase 2 `nyct-gtfs` collector) may still be worth doing for other reasons, but OTP's real-time subway matching itself is not blocked on it. | n/a |

**Anthropic spend cap:** user requested a **$5/month hard cap**. There is no API to set this — **user must set it manually** at console.anthropic.com under Settings > Limits; I cannot verify it's been set. As a second line of defense, `backend/cost_guard.py` logs every call's cost locally and (a) raises before further calls once month-to-date spend hits the cap, (b) prints an alarm if a single day's spend implies burning the budget 3x+ faster than sustainable. This is a local circuit breaker only — it does not replace the console cap, which is the authoritative stop.

Before touching credentials/keys in code or docs, re-check this table is still current — it's a snapshot, not a live source.

## Build order (reprioritized 2026-08-14 — deviates from the roadmap plan's Task 1-7 order)

User explicitly prioritized bus corridor data collection over the rest of Phase 1, since bus reliability has no retroactive historical source (subway does, via subwaydata.nyc) and needs ~2-3 weeks of live observation before any probability is meaningful. Actual build order:

1. **Done:** `backend/collectors/bus_collector.py` — polls Q70+/M60+ GTFS-RT every 30s (kept at spec's §5.2 cadence deliberately — headways/delays are minutes-scale so 30s adds negligible noise, and the repeated re-prediction across cycles is what feeds the `predicted_arrival_ts_at_T_minus_5` prediction-error stat, not just passage timing). Writes raw ndjson to `backend/data/raw/bus/YYYY-MM-DD.ndjson` (gitignored, local only), retry+backoff on failure, and gzips each completed day once the date rolls over (today's file is never touched mid-write) — measured 31x compression on real data (6.5MB -> 205KB), so the earlier ~6GB/3-week estimate is actually closer to ~200MB compressed.
2. **Persistence solved, with a caveat:** Task Scheduler and the `ScheduledTasks` PowerShell module are both `Access is denied` on this account (a real OS restriction, not a sandboxing artifact — confirmed with a trivial notepad.exe test task). Used the Windows **Startup folder** instead (`IsMyTrainScrewed-BusCollector.lnk` in `shell:startup`, launching `backend/run_bus_collector.bat`) — functionally equivalent for surviving this session ending and reboots, verified by observing the process outlive its own launching process. **Gap:** won't collect while fully logged out (no one logged in) — that would need stored Windows credentials for "run whether logged on or not," which needs the same blocked permission. Fine for a daily-use laptop.
3. Restart procedure after editing `bus_collector.py`: find the running `python.exe` via `Get-CimInstance Win32_Process -Filter "Name='python.exe'" | Where-Object { $_.CommandLine -like "*bus_collector*" }`, `Stop-Process -Force`, then relaunch via `Start-Process -FilePath backend\run_bus_collector.bat -WindowStyle Hidden` — the running process does not hot-reload code changes.
4. Original Task 1-7 (FastAPI scaffold, OTP, nearest-stop index, PWA shell) resumes after the collector is confirmed stable — not blocked by anything except the static GTFS URL confirmation (Task 2).
5. **Done 2026-08-15: bus collector deployed to Railway, laptop dependency removed.** Local process stopped, Startup-folder autostart entry removed — the lid-close question from 2026-08-14 is now moot. Historical local data at `backend/data/raw/bus/*.ndjson[.gz]` (collected 2026-08-14 through 2026-08-15 07:42 UTC) is preserved, not deleted — it predates Railway's collection and needs to be merged in during Phase 2 aggregation, not treated as redundant.

   **Railway deployment details:**
   - Project `reliable-courtesy` (id `77096939-d30b-46a4-b439-c545aff3fe25`), service `is-my-train-screwed` (id `91c6a5c2-7c04-46d0-9c01-24b17b9c4014`), environment `production` (id `95c28f31-9ee7-465f-9046-034215422795`) — all pre-created by the user before handing over the token.
   - Source: connected to GitHub `hardmoneysniper/is-my-train-screwed`, branch `main`, `rootDirectory: backend` — pushes to `main` should trigger auto-redeploy (assumed from standard Railway repo-connection behavior; not yet verified with an actual post-connection push).
   - Config: `buildCommand: pip install -r requirements.txt`, `startCommand: python collectors/bus_collector.py`, `restartPolicyType: ALWAYS`, **`sleepApplication: false`** (critical — this worker has no HTTP endpoint, so Railway's traffic-based auto-sleep would otherwise kill it).
   - Persistent volume `is-my-train-screwed-volume` mounted at `/app/data` (survives redeploys/restarts; without this, every redeploy would silently wipe collected data).
   - Only env var set on Railway: `MTA_BUSTIME_API_KEY`. Nothing else needed for the collector.
   - Verified live 2026-08-15: build succeeded, container started, log lines confirmed growing on the correct 30s cadence for 3+ consecutive cycles before the local collector was decommissioned (no coverage gap).

   **Gotchas hit during this deploy, useful if repeating for another service:**
   - Railpack (Railway's current default builder) only auto-detects Python via the exact filename `requirements.txt` — a `requirements-collector.txt` caused it to skip Python provisioning entirely and run the build command in a bare shell (`pip: not found`). Renamed to `requirements.txt` (there's now one canonical requirements file in `backend/`, not split by purpose).
   - `serviceInstanceUpdate` does **not** clear a field when you pass `null` — it's silently ignored, not unset. Setting `buildCommand: null` left the old (wrong) command in place; had to overwrite with the correct explicit string instead.
   - **This specific token (a personal token scoped to a workspace, not a bare account or project token) does not work with the Railway CLI's convenience commands** (`railway link`, `status`, `variables`, `whoami`, `up` all return "Unauthorized"/"Invalid RAILWAY_TOKEN") **but works fine for raw GraphQL** via `railway api "<query>"` or direct `curl` to `https://backboard.railway.app/graphql/v2` with `Authorization: Bearer <token>`. Everything above was done via `railway api` mutations/queries, not the higher-level CLI subcommands. If future Railway work needs the convenience commands, generate a plain account-level token instead — don't waste time re-debugging this.
   - Env var in `.env` is `RAILWAY_API_TOKEN` (matches actual usage), not `RAILWAY_TOKEN` (that name is reserved for Railway's project-scoped token type, which this isn't).
   - **2026-08-15: got a second, genuinely account-level personal token** (`me` query works, CLI convenience commands like `whoami`/`list` work with it) — stored as `RAILWAY_API_TOKEN` now; the original workspace-scoped one is kept as `RAILWAY_WORKSPACE_TOKEN` since it's already proven to work for `railway api` project/service mutations and there's no reason to re-verify everything against the new one.

   **Cost verification (user has $5 trial credit, asked to confirm this deployment is free):**
   - `defaultPaymentMethodId: null` on the billing customer — **no card on file**, confirmed via GraphQL. Railway cannot charge real money without one.
   - Attempted to set an explicit hard usage limit as a belt-and-suspenders safety net (`usageLimitSet` mutation) — Railway itself rejected it: `"Usage limits require an active subscription."` This account is pure prepaid trial ($5/30 days, `isTrialing: true`, `state: INACTIVE` = no active paid subscription), so the usage-limit feature doesn't even apply — stronger guarantee than a self-imposed cap, since there's no billing relationship to exceed in the first place. **If a payment method is ever added later, revisit this** — the "can't be charged" guarantee goes away at that point.
   - Real measured resource usage via Railway's `metrics` query (not estimated): ~0.00095 vCPU (~0.1% of one core), ~19.6MB RAM. At published rates (verified live via railway.com/pricing, not memory: $0.028/vCPU-hour, $0.014/GB-hour, $0.22/GB-month for volumes) that's roughly **$0.007-0.02/day all-in** including volume growth — $5 covers this comfortably for 250+ days, far beyond the 2-3 week collection window needed.

6. **2026-08-15: volume-fill and data-loss questions investigated.** User's premise was Railway auto-deletes data if the volume gets to ~6GB — didn't confirm that (no evidence for it, and no such behavior is documented or expected for block-storage volumes in general: filling up should mean writes fail with ENOSPC, not silent deletion of existing data). What I *did* find, worth knowing regardless:
   - **The Trial-plan volume cap is 500MB (434MB usable), not 6GB** — much smaller than assumed. Confirmed via live SSH (`df -h /app/data`), not the GraphQL `currentSizeMB` field, which turned out to be a stale/batched metric (unchanged across 15+ min of active writes) — don't trust it for anything time-sensitive.
   - Real intraday growth rate (measured via SSH `ls -la`, not extrapolated): ~123MB/day pre-compression. Since gzip rotation only fires at day-rollover, a single day's uncompressed file could peak near 123MB before compressing to ~4MB — comfortably under the 434MB cap even in the worst case, so no urgent fix was needed to the collector itself.
   - `VolumeUpdateInput` (the resize mutation) only exposes a `name` field, no size — matches the docs saying volume resize is a paid-plan-only, dashboard-driven feature not available on Trial via this API.
   - **Built a local backup safety net anyway** (`backend/scripts/backup_from_railway.py` + `backend/run_railway_backup.bat`), since independent redundancy is worth having regardless of the (unconfirmed) auto-delete premise — protects against volume/project loss, account issues, or a future write failure if the cap is ever actually hit. Streams `tar -czf - -C /app/data raw` over `railway ssh` to a local timestamped `.tar.gz` under `backend/data/backups/`. Checks a local marker file (`backend/data/.last_railway_backup`) and only actually runs if 5+ days have passed since the last backup — same Task-Scheduler-is-blocked constraint as the old collector autostart, so it's wired to the **Startup folder** (`IsMyTrainScrewed-RailwayBackup.lnk`) and re-evaluates the 5-day condition on every logon rather than firing on a true interval. Verified end-to-end 2026-08-15: real archive pulled and extracted successfully, marker correctly suppressed a second immediate run.
   - Needed an SSH key registered with Railway to make this work (`railway ssh keys add`) — reused the existing local `~/.ssh/id_ed25519` rather than generating a project-specific one.
   - Gotcha: `subprocess.run(["railway", ...])` fails on Windows with `FileNotFoundError` — `railway` installs as a `.CMD` npm shim, which Windows `CreateProcess` won't exec directly without going through `cmd.exe`. Fixed by resolving the real path via `shutil.which` and prefixing `["cmd", "/c", ...]` on `os.name == "nt"`.

## Reminders for future sessions

- Re-verify MTA endpoint URLs against current MTA developer documentation if it's been a while — they've changed before and the spec explicitly warns not to trust training data here.
- Follow the frontend-design skill before/while building any UI — this product must not look like a default AI chat app (spec §9).
- Any model-routing change must be justified against the `evals/` fixture set (~30 anonymized transcripts) comparing tool-call accuracy across models — no routing changes on vibes (spec §9.1).
